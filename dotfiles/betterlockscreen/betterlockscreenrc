# === ОБОИ ===
# Путь к изображению (поддерживает ~/)
WALLPAPER="$HOME/Pictures/wallpaper.jpg"

# === ЭФФЕКТЫ ===
# Варианты: blur, pixelate, dim, none
EFFECT="blur"

# Сила размытия (только для blur)
BLUR_SIGMA="10"

# Сила пикселизации (только для pixelate)
PIXELATE="5"

# Яркость затемнения (только для dim, от 0.0 до 1.0)
DIM="0.3"

# === ПОВЕДЕНИЕ ===
# Кэшировать обработанное изображение? (ускоряет последующие блокировки)
CACHE="true"

# Пересоздавать кэш при изменении обоев?
CACHE_UPDATE="true"

# Использовать i3lock-color (обязательно для эффектов)
LOCKER="i3lock-color"

# === ДОПОЛНИТЕЛЬНЫЕ ФЛАГИ I3LOCK ===
# Например: показать часы, скрыть ввод пароля и т.д.


